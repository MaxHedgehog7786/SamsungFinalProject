<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/white"> <!-- Цвет кружочка при нажатии -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/white" /> <!-- Цвет кружочка при нажатии -->
            <corners android:radius="0dp" /> <!-- Радиус скругления краев, если требуется -->
        </shape>
    </item>
</ripple>
